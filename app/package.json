{
  "name": "my-app",
  "private": true,
  "scripts": {
    "preinstall": "cargo install wasm-pack",
    "dev": "yarn wasm && next dev",
    "build": "yarn wasm && next build",
    "start": "yarn wasm && next start",
    "export": "yarn wasm && next build && next export",
    "wasm": "wasm-pack build --target web wasm",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "*",
    "react": "*",
    "react-dom": "*"
  },
  "devDependencies": {
    "@types/node": "*",
    "@types/react": "*",
    "eslint": "*",
    "eslint-config-next": "*",
    "typescript": "*"
  }
}

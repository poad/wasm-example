{
  "name": "@cloudfront-cdn-template/function",
  "version": "0.1.0",
  "bin": "bin/cloudfront-cdn-template.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "eslint --ext .ts lambda bin lib",
    "style-check": "prettier --write lambda bin lib",
    "format": "prettier --write lambda bin lib",
    "lint-fix": "prettier --write lambda bin lib && eslint --fix --ext .ts lambda bin lib"
  },
  "devDependencies": {
    "typescript": "^5.4.4"
  },
  "engines": {
    "npm": "use pnpm please!",
    "yarn": "use pnpm please!",
    "pnpm": ">=8.7.1",
    "node": ">=20.0.0"
  }
}
